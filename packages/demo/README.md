# Demo

The easiest way to run this demo is with [devenv](https://devenv.sh/getting-started/). After setting
up devenv, cd into this pitter-patter repo to activate the environment. Then run `devenv up` to run
the demo server. By default the demo will be available on port 3000.
